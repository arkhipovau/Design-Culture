# A_Mail_input

Mail input atom with 3 visual states:
- `default` (placeholder / dark-grey)
- `input` (entered value / black)
- `success` (success text / dark grey)

Use attribute `data-state` on input element.
